%p#notice= notice
%h1= t("gamfora.game.views.index.header")
%table#games
  %thead
    %tr
      %th Name
      %th{:colspan => "3"}
  %tbody
    - @games.each do |game|
      %tr{:id => "game_game.id"}
        %td= game.name
        %td= link_to t("gamfora.buttons.show"), game
        %td= link_to t("gamfora.buttons.edit"), edit_game_path(game)
        %td= link_to t("gamfora.buttons.destroy"), game, method: :delete, data: { confirm: t("gamfora.game.views.destroy.question", name: game.name) }
      
%br/
= link_to t("gamfora.game.views.index.button_new"), new_game_path
